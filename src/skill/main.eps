/*
스킬 큐는 각 플레이어 객체에 저장된다.
간단하게 하기 위해서 1이 Q, 2가 W, 3이 E, 4가 힐, 5가 인포. 가시성 개구리지만 어차피 내 알 바 아님(?)
*/

import player.player;
import player.skill;
import skill.buildQueue;
import skill.useSkill;

function skillLoop() {
    buildQueue.loop();
    useSkill.loop();
}
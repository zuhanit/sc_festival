import player.player;
import player.skill;

function loop() {
    for (var p = 0; p < 6; p++) {
        setcurpl(p);
        const obj = player.Player.cast(player.players[p]);
        obj.skillFuncPtr();
    }
}
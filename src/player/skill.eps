import skill.using;

object Skill {
    var q;
    var w;
    var e;
    var qCooldown;
    var wCooldown;
    var eCooldown;

    function using() {
        var k = 0;
        if (this.q > 0) {k = 1;}
        if (this.w > 0) {k = 2;}
        if (this.e > 0) {k = 3;}
        return k;
    }
};